## LLxprt Code Added Memories
- CRITICAL: NEVER delete, remove, or modify the .llxprt/ directory or any of its contents (LLXPRT.md, settings.json, skills/, commands/). This directory contains project memories, settings, and skills that are version-controlled. Do not run rm, git clean, git checkout, or any other command that would remove these files. If git status shows .llxprt files as modified, leave them alone — they are supposed to be there. "Clean workspace" means only: checkout main and pull from origin. It does NOT mean deleting files.
- This is a Rust project (vybestack/llxprt-jefe). Before checking in code changes from the main project directory, run the full verification suite: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo build --workspace --all-features --locked`, and `cargo test --workspace --all-features --locked` — or simply `make ci-check` which runs fmt check, the clippy gates, coverage (--fail-under-lines 30), build, and test. Fix any errors and ensure code is formatted before pushing/creating PRs. Always use gh for PRs/issues/comments (never webfetch). For fast iteration use `make quick-check` (cargo fmt + cargo check -q + cargo test -q).
- When user requests to address GitHub issue #NUM: 1) Checkout main and pull latest from origin (do NOT delete any files or run git clean — just git checkout main && git pull), 2) Create branch "issueNUM", 3) Use gh to pull issue and comments, 4) Research issue in codebase using description/comments as starting point, 5) Create test-first plan following dev-docs/RULES.md behavioral tests (RED -> GREEN -> REFACTOR, no mock theater), respecting the module boundaries in dev-docs/project-standards.md (UI renders/emits intent, app state/event layer owns transitions, runtime layer owns tmux/PTY orchestration, persistence owns file I/O), 6) Delegate to rustcoder subagent with exquisite detail to implement (must run full verification: cargo fmt --all --check, cargo clippy --workspace --all-targets --all-features -- -D warnings, cargo build --workspace --all-features --locked, cargo test --workspace --all-features --locked, i.e. make ci-check), 7) Have rustreviewer (and deepthinker for harder issues) review for compliance and issue intent (run verification cycle), 8) If fails: remediate with rustcoder (giving feedback, run verification), then rustreviewer review again - loop until satisfied, 9) Add/commit/push and use gh to create PR with exquisite detail including "closes #NUM" or "fixes #NUM" in the body and the issue number in the title, 10) Use gh watch with 5m delay until workflows finish - address coderabbit comments/fix failures (using same subagents), run verification cycle, add/commit/push, watch again - loop until all pass, 11) Use proper gh command syntax (avoid unescaped backticks), 12) NEVER exit to prompt telling user workflows are still running - must watch, loop, fix, watch until complete.
- Rust quality baseline (from dev-docs/RULES.md and project-standards.md): TDD is mandatory; prefer explicit domain types over primitive obsession; use Result/Option and typed errors; no unwrap/expect in production paths; unsafe is forbidden unless explicitly approved and isolated; keep side effects at boundary modules and state-transition/reducer logic deterministic.
- When starting work on a new issue in this project, create a dedicated issue branch such as issue72 before making changes.
- For any new feature exposed through the UI, use test-first coverage that includes both UI/unit behavior tests and TUI harness scenario scripts. Create/update the TUI scenario first and prove the relevant test/scenario fails before implementing the feature.
- Issue #202 design (unify list pagination): introduce `PaginatedList<T, I>` generic deterministic state container in `src/state/pagination.rs` + `PageToken`/`ListRequestId` value types in `src/domain/pagination.rs`. NO trait objects, NO `dyn Any`, NO `ListLoader` service in services/. PageToken enum {Cursor(String), PageNumber(u32), Done} unifies GraphQL-cursor (Issues/PRs) and REST-page (Actions) pagination. has_more derived from token (not stored). Single PendingLoad<I> enum {Reload{identity,request_id,visibility}, Page{identity,token,request_id}}. AcceptOutcome {Applied, Empty, Stale}. ReloadVisibility {Visible, Silent} for PR silent refresh. Per-screen identity structs (ActionsListIdentity, IssueListIdentity, PrListIdentity). REMOVE request_id==0 legacy special-case. PR scope strategy: ship abstraction + Actions migration in PR#1 (Actions gains working load-more), Issues migration PR#2, PRs migration PR#3. Delete dead Actions `page>1` extend branch in reload_runs().
