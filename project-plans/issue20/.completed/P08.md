# P08 — GitHub Client Impl (GREEN) — COMPLETE

Plan: PLAN-20260624-PR-MODE, Phase 08
Status: COMPLETE (GREEN)

## Summary
Replaced the total-stub bodies in `src/github/parse_pr.rs` and the 6 PR stub
methods in `src/github/mod.rs` with real GraphQL arg-building, JSON parsing,
error mapping, and send-payload assembly. The 29 behavioral PR unit tests added
in P07 now pass.

## Implementation
- `src/github/parse_pr.rs` (~+580): 13 public helpers (all < 60 lines) + named
  splits — build_pr_comments_query, build_pr_search_args, build_pr_search_query,
  parse_pull_requests_json, parse_pr_from_node, parse_pull_request_detail_json,
  parse_pr_review, parse_pr_check, parse_review_decision, parse_check_status,
  parse_checks_rollup, parse_pr_state, sort_pull_requests + helper splits
  (parse_review_state_token, rollup_nodes, pr_state_qualifier,
  review_decision_qualifier, checks_status_qualifier, pr_string_array, str_field,
  login_field, join_pr_nodes_field, merged_at_non_empty).
- `src/github/mod.rs` (+308/-9): list_pull_requests, get_pull_request_detail,
  list_pr_comments, create_pr_comment, open_pull_request_in_browser,
  build_pr_send_payload, private run_gh helper, PR_LIST_PAGE_SIZE=30.
- `src/github/parse.rs` (+18/-3): parse_comments_json accepts BOTH issue and
  pullRequest comment paths; parse_page_info raised to pub(super) for reuse.
- `src/github/tests.rs`: split test_parse_pr_list_maps_all_fields into two
  (added test_parse_pr_list_maps_second_pr_merged_and_failure) to satisfy the
  CI cognitive_complexity<=15 gate; all original assertions preserved.

## Critical correctness
- list_pr_comments / build_pr_comments_query target
  `repository.pullRequest(number:).comments` NOT `repository.issue(number:)`
  (P00A §2d: issue(<PR number>) is NULL → silent-empty regression). Proven by
  test_list_pr_comments_query_targets_pull_request_not_issue (GREEN).
- get_pull_request_detail sources first comment page via a separate
  list_pr_comments call (its gh pr view --json omits comments).
- statusCheckRollup parser handles BOTH CheckRun and StatusContext shapes,
  degraded-never-dropped.

## Verification (all GREEN)
- cargo fmt --all --check
- cargo clippy --workspace --all-targets --all-features -- -D warnings
- CLIPPY_CONF_DIR=.github/clippy cargo clippy ... -D cognitive_complexity
  -D too_many_lines -D too_many_arguments -D type_complexity
  -D struct_excessive_bools (the CI complexity gate)
- bash scripts/check-clippy-allows.sh → passed
- bash scripts/check-architecture.sh → passed
- cargo build --workspace --all-features --locked
- cargo test --workspace --all-features --locked → 383 lib + 89 + 199 + 0 doc, 0 failed
- smell/deferred/config gates clean
