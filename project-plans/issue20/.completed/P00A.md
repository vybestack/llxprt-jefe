# P00A — Preflight Verification: PASS

- Plan ID: PLAN-20260624-PR-MODE
- Phase: P00A (standalone Phase 0.5 grounding gate; no production code)
- Timestamp: 2026-06-15

## Toolchain
- cargo 1.92.0 (Homebrew); rustc 1.92.0; clippy 0.1.92
- cargo-llvm-cov 0.8.5 PRESENT (coverage gate runnable locally)
- gh 2.83.2, authenticated as acoliver (keyring)

## Structural checks — PASS
- All target files/dirs exist (state/types.rs, state/mod.rs, input.rs, messages.rs,
  messages/issues_conversion.rs, app_input/{mod,normal,gh_async}.rs, github/mod.rs, layout.rs,
  ui/orchestration.rs, ui/screens/issues.rs).
- New identifiers confirmed ABSENT: DashboardPullRequests, PullRequestsState,
  MessageDomain::PullRequests, apply_prs_message.

## Signature grounding (current lines) — PASS
- ScreenMode enum L227 (Dashboard, Split, DashboardIssues); AppState L247; issues_state L277;
  IssueFocus L285; DetailSubfocus L296; IssuesState L368.
- input.rs: InputMode L9; input_mode_for_state L45; DashboardIssues block L64; route_search_key L89.
- messages.rs: MessageDomain L18; IssuesMessage L113; AppMessage L283; From<AppEvent> L483.
- state/mod.rs: apply_message L342; select_repository_by_index L488; reset_issues_for_repo_change L497.
- app_input: dispatch_app_message L420; apply_and_persist L216; handle_normal_key_event L85;
  handle_dashboard_issues_key L156; resolve_mode_key L296; spawn_gh_task_with_panic gh_async L11.
- github/mod.rs: GhError L31; GhClient L95; list_comments L211; create_comment L315.
- layout.rs: ISSUES_SIDEBAR_WIDTH L206; issues_pane_rows L234; issues_detail_viewport_rows L264
  (list_first_visible_index/list_visible_window/prs_pane_rows confirmed ABSENT — to be added).
- orchestration.rs: build_screen_element L75; DashboardIssues arm L91.

## Persistence grounding — PASS
- to_persisted_state (app_input/mod.rs L115) enumerates exactly 8 fields and OMITS issues_state;
  prs_state will likewise be transient/runtime-only (AppState is non-serde). Backward-compat is
  achievable as planned.

## Live GitHub groundings — PASS (CRITICAL)
- §2d repository.issue(89) => NULL (NOT_FOUND for a PR number) — confirms reusing issue
  list_comments for PRs would silently return zero. list_pr_comments MUST use repository.pullRequest.
- §2d repository.pullRequest(89).comments => real connection (totalCount=1, pageInfo present).
- §2b draft:true server-side => 158 PRs, all isDraft=true (pagination-preserving qualifier confirmed).

## Quality-gate baseline (5-command) — GREEN
- cargo fmt --all --check: OK
- cargo clippy --workspace --all-targets --all-features -- -D warnings: clean
- bash scripts/check-clippy-allows.sh: passed
- cargo build --workspace --all-features --locked: OK
- cargo test --workspace --all-features --locked: all suites green

## Blocker Gate Decision: PASS — plan grounded; no drift requiring revision.
