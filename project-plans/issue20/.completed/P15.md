# P15 — PR-Mode Integration-Test Hardening

- **Plan ID:** PLAN-20260624-PR-MODE
- **Phase ID:** P15
- **Status:** PASS (committed 8593cd2)

## Summary
Added 19 integration checkpoints spanning key-routing, dispatch, persistence,
and regression locks, placed in the correct crate so the REAL production seams
are exercised:
- `src/app_input/prs_integration_tests.rs` (binary crate) drives the REAL
  `to_persisted_state` (cp17 persistence exclusion) and REAL
  `prs::resolve_prs_key_event` (cp10 Esc-precedence chain), plus send-to-agent,
  open-in-browser command shape, non-blocking I/O, and config-error checkpoints.
- `src/state/prs_integration_tests.rs` (library crate) covers reducer/state
  checkpoints incl. cp7 composer viewport-follow regression lock (#56).
- `src/app_input/mod.rs` + `src/state/mod.rs`: `#[cfg(test)]` registration only.

## Verification
- Five-command baseline + complexity clippy + architecture + make ci-check: green.
- 423 lib + 134 bin + 199 integration = 756 tests, 0 failures.
- cp7/cp10/cp17 mutation-proven load-bearing; production restored exactly.
