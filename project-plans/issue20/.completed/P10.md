# P10 — Message-Bus + Key-Routing RED (TDD) — DONE

Plan: PLAN-20260624-PR-MODE, Phase 10 (RED of stub(P09)→RED(P10)→GREEN(P11))

## Scope delivered
- 27 behavioral key-routing tests appended to `src/app_input/prs.rs`
  (`#[cfg(test)] mod tests`), mirroring the `issues.rs` test precedent with
  local helpers (`key`, `key_with_mods`, `prs_base_state`,
  `prs_state_with_focus`, `prs_state_with_inline`, `prs_state_with_chooser`,
  `prs_state_with_search_focused`, `prs_state_with_filter_open`).
- Tests assert EMITTED `AppEvent`s from `resolve_prs_key_event` /
  `handle_prs_mode_key` per component-003 precedence. 22 are RED against the
  P09 stubs; 5 are legitimately GREEN (the two `o` open-in-browser tests, the
  two `p` entry tests routed through the REAL `resolve_mode_key`, and the
  suppression consumed-no-op test).
- Entry test (`test_p_from_dashboard_emits_enter_prs_mode`) calls the real
  `super::super::normal::resolve_mode_key` for both `p` and `P` from
  `ScreenMode::Dashboard` and asserts `KeyHandling::Handled(Some(EnterPrsMode))`
  — green-for-the-right-reason (exercises production routing).
- Esc precedence test covers ALL tiers including the two search cases:
  search-focused + nonempty ⇒ `PrClearSearch`; search-focused + empty ⇒
  `PrBlurSearchInput` (component-003 ~L95-96) — currently RED against the stub.
- `AppEvent` lacks `PartialEq`/`Debug`; all assertions use `matches!(...)`.

## Production change (test-enablement only)
- `src/app_input/normal.rs`: widened `enum KeyHandling` and
  `fn resolve_mode_key` from private to `pub(super)` so the entry-routing test
  can exercise the real router. No behavioral change.

## Verification (RED phase rules)
- GREEN: fmt, `clippy -D warnings`, CI complexity clippy pass, check-clippy-allows,
  build. RED: `cargo test` bin target 94 passed / 22 failed (all behavioral
  assertion mismatches, no panics/compile errors); lib 383/0.
- Weak-smell gate empty: `assert!(true)|#[ignore]|.unwrap()|.expect()` none in
  `src/app_input/`. No `todo!()`/`unimplemented!()`. No `#[allow]`.
- Traceability: every test carries `@plan PLAN-20260624-PR-MODE.P10`,
  `@requirement REQ-PR-*`, `@pseudocode component-003 lines X-Y`.
- No `AppEvent`↔`PullRequestsMessage` conversion tests (owned by P04/P05).
- No config drift (Cargo.toml/clippy.toml/.github/clippy unchanged); no
  `.config/jefe-dev/state.json` committed.

## Files
- Modified: src/app_input/prs.rs (appended test module), src/app_input/normal.rs
  (two `pub(super)` visibility widenings).
