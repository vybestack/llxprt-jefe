# P05 — Domain & State Impl (GREEN) — COMPLETE

Plan: PLAN-20260624-PR-MODE.P05
Phase: 05 (Domain & State Impl — turns P04 RED tests GREEN)

## Outcome
All 35 P04 RED behavioral tests now pass via genuine production reducer implementations. Full baseline GREEN with no overrides.

## Deliverables
- Implemented PR-mode reducers across src/state/prs_ops.rs, prs_nav_ops.rs (split out of prs_ops to satisfy the 850-line handler limit), prs_load_ops.rs, prs_inline_ops.rs, prs_mutation_ops.rs.
- Implemented message<->event conversion in src/messages/prs_conversion.rs (PullRequestsMessage::from_app_event + From<PullRequestsMessage> for AppEvent), total over PR variants.
- Implemented shared viewport helpers list_first_visible_index / list_visible_window in src/layout.rs (consumed by state; UI consumes in P14).
- Shared AppState::move_repo_selection helper in src/state/mod.rs; BOTH issues navigate_repo_up/down_in_issues_mode AND PR navigate_repo_up/down_in_prs_mode delegate to it (duplication removed — Finding 5).
- select_repository_by_index resets prs_state when active (reset_prs_for_repo_change).
- apply_message PullRequests arm asserts debug_assert!(handled, ...).

## Verification (independently confirmed + rustreviewer PASS)
- cargo fmt --all --check: PASS
- cargo clippy --workspace --all-targets --all-features -- -D warnings: PASS (0 warnings)
- cargo build --workspace --all-features --locked: PASS (0 warnings)
- bash scripts/check-clippy-allows.sh: PASS
- cargo test --workspace --all-features --locked: 353 + 89 + 199 + 0 = 641 tests, 0 failed
- bash scripts/check-architecture.sh: PASS (all PR-mode *ops.rs under 850; form_ops.rs uses its existing script exemption)
- Forbidden-macro + deferred-marker inverted gates: clean
- Per-fn traceability marker audit: 0 MISSING across all 6 PR state/conversion files + layout helpers
- clippy.toml / .github/clippy/clippy.toml / Cargo.toml / scripts: unchanged (git diff empty)
- P04 test files unchanged since b98129a (git diff empty)

## Reducer purity
No I/O, spawn, file writes, or crossterm::size()/terminal-size calls in src/state/prs_*.rs; scroll math bounded by stored viewport props in state.
