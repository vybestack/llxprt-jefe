# Phase P04 — Domain & State TDD (RED) — COMPLETE

- Phase ID: PLAN-20260624-PR-MODE.P04
- Timestamp (UTC): 2026-06-25T16:50:58Z
- Worker: rustcoder

## Files created (test-only)
- src/state/prs_tests.rs, prs_tests_detail.rs, prs_tests_filter.rs,
  prs_tests_repo_nav.rs, prs_tests_composer_focus.rs, prs_tests_detail_flow.rs,
  prs_tests_components.rs (38 behavioral tests)
- 3 viewport-helper tests added to src/layout.rs #[cfg(test)] mod tests
- 3 persistence backward-compat tests (test_to_persisted_state_excludes_prs_state
  in src/app_input/app_input_tests.rs; pre-pr deserialize + default-inactive in prs_tests.rs)
- Registrations via #[path] in src/state/mod.rs

## RED-phase gate result
- cargo fmt --all --check: PASS
- cargo clippy ... -D warnings: PASS
- bash scripts/check-clippy-allows.sh: PASS
- cargo build --workspace --all-features --locked: PASS (tests compile)
- cargo test --workspace --all-features --locked: RED — 318 passed; 35 failed (all by ASSERTION, never panic)
- weak-test inverted gate: clean (no assert!(true)/#[ignore]/.unwrap()/.expect())
- all test fns carry @plan/@requirement/@pseudocode component-NNN markers
- production reducer/type/conversion/layout BODIES untouched (test-only diffs)

## Semantic verification summary
RED tests pin P05 behavior: enter/exit + prior-focus restore, default-open filter,
focus cycle, repo nav independent of pane focus (#47), repo-scope reset,
list renders all rows (#54), list/detail staleness discard by scope AND request_id (NFR-002),
page append no-reorder, composer NewComment subfocus + follow viewport (#56),
filter draft-only update + apply/cycle/search (#38/#40), scroll bounded by rendered length (#37/#39),
agent chooser, ShowNotice per ReadOnlyHintKind, open-in-browser purity, empty-list clears seeded state.
Reducer-purity tests route via the real hub (apply_message). 

## Decision: PASS
