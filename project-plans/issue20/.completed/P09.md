# P09 — Message-Bus + Key-Routing STUB — DONE

Plan: PLAN-20260624-PR-MODE, Phase 09 (stub of stub→RED(P10)→GREEN(P11))

## Scope delivered (per normative S1–S5 ruling in 09-stub)
- Full 8-tier key-routing precedence STRUCTURE compiling, mirroring
  resolve_issues_key_event (S1): P1–P4 early returns, then P5 global
  .or_else P6 focus .or_else P7 pane-cycle .or_else P8 suppression.
- Explicit P8 suppression tier (S2): resolve_pr_suppressed_key +
  is_pr_suppressed_key identify reserved s / l / Ctrl-d / Ctrl-k and
  consume (None); wired after P7.
- REQ-PR-012 `o` open-in-browser (S3): implemented in handle_pr_list_key
  and handle_pr_detail_key only — Some(PrOpenInBrowser) when target present
  else Some(PrShowNotice(NoSelectionToOpen)); ABSENT from
  resolve_pr_global_key; PullRequests(OpenInBrowser) dispatch arm wired.
- Esc delegation (S4): handle_esc_in_prs_mode present/inert in
  resolve_pr_global_key, which matches only Esc at stub.
- All other mappings DEFERRED (S5): p|P / a / ?|h|F1 / / / f, nav, pane-cycle
  (Tab/Shift+Tab), j/k subfocus, r/c/e notices, S chooser — all return None
  at stub so P10 RED tests stay red. The P7 pane-cycle resolver exists for
  S1 structure but is inert (returns None).
- Message dispatch router dispatch_prs_message + loader/send-to-agent stub
  set in mod.rs (canonical refresh_prs_navigation; singular absent).
- Send-to-agent skeleton: pr_send_info, write_pr_prompt, launch_pr_agent
  (inert — no runtime spawn this phase), wired via
  dispatch_pr_agent_chooser_confirm mirroring the Issues chain.

## Files
- New: src/app_input/prs.rs, prs_dispatch.rs, prs_filter.rs,
  prs_list_dispatch.rs, prs_mutation.rs
- Modified: src/app_input/mod.rs (dispatch routing + stub fns),
  src/app_input/normal.rs (p/P entry + handle_dashboard_prs_key)

## Gates (all GREEN, independently run)
fmt; clippy -D warnings; CI complexity-clippy (cognitive_complexity,
too_many_lines, too_many_arguments, type_complexity, struct_excessive_bools);
check-clippy-allows (passed); check-architecture (passed); build;
test (383 lib + 89 + 199 + 0 doc, 0 failed). No todo!()/unimplemented!();
no #[allow]/#[expect]; config files unchanged (CONFIG-OK). All new
fns/structs carry @plan / @requirement / @pseudocode markers.
