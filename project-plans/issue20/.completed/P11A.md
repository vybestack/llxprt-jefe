# P11A — Message-Bus + Key-Routing GREEN — VERIFICATION PASS

Plan: PLAN-20260624-PR-MODE, Phase 11 verification.

## Verdict: Phase 11: PASS (zero blockers)

Independent rustreviewer behavioral verification confirmed:

- RED→GREEN: 707 passed / 0 failed across all targets (lib 384, bin 124,
  integration/core 199, doc 0). All prior-RED PR key-routing + dispatch-ordering
  tests green.
- HANDLER CORRECTNESS: per-pane arrow pane-cycle (RepoList Right; PrList
  Left+Right; PrDetail Left, Right unbound), Tab/BackTab, read-only
  Some(PrShowNotice) never None, c/r composer, S chooser, o open/notice, full Esc
  precedence chain — all cited in src/app_input/prs.rs. input_mode_for_state real
  precedence (src/input.rs); normal.rs prs-before-mode ordering; filter controls
  fully interactive (src/app_input/prs_filter.rs).
- INLINE-SUBMIT ORDERING: Inline(Submit) arm runs apply_and_persist BEFORE
  handle_pr_inline_submit (mod.rs:565-568); reducer sets mutation_pending +
  preserves text (prs_inline_ops.rs:251-274); resolver requires it
  (prs_mutation.rs:56-75); regression test asserts the precondition.
- OPEN-IN-BROWSER: NoSelection→notice (no spawn); InvalidSlug→PrOpenInBrowserFailed
  (never silent, exact message); Ok→off-thread spawn; reducer-before-spawn.
- NFR-001: all 5 gh loaders inside spawn_gh_task_with_panic with panic→*Failed;
  no UI-thread blocking.
- SEND-TO-AGENT: launch_pr_agent real (spawn_and_attach_fresh_for_pr ctx==None
  guard; persist_pr_agent_launch_success; apply_pr_send_to_agent_failed);
  chooser-confirm ordering read-info→apply→write-prompt→launch.
- VIEWPORT: prs_detail_viewport_rows exists by name (layout.rs:280-298), called
  by update_pr_detail_viewport_rows.
- GATES: fmt, clippy -D warnings, CI complexity clippy, check-clippy-allows,
  check-architecture, build all exit 0. Hard gates empty (src/ scope).
- CONFIG INTEGRITY: no diff to Cargo.toml / clippy.toml / .github/clippy /
  scripts/check-architecture.sh. Comments-loading extracted to new
  prs_comments_dispatch.rs (no architecture override).
- TRACEABILITY: all P11-scoped fns + new tests carry the three markers; no
  improperly stale earlier-phase markers.

## Resolved nit
- The new `prs_comments_dispatch.rs` was untracked at review time; staged and
  committed as part of P11.
