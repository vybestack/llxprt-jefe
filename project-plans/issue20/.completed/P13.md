# P13 — PR-Mode UI TDD (RED)

- **Plan ID:** PLAN-20260624-PR-MODE
- **Phase ID:** P13
- **Status:** PASS (committed 61722cf)

## Summary
Authored behavioral render tests for the PR-mode UI as RED/regression locks:
- Regression locks (#54 all rows render, #55 selected row stays visible) via the
  pure selection-follow projections consumed by `pr_list`.
- RED scroll-clamp tests proving detail overflow must derive from REAL rendered
  length, not a heuristic (#37f), use the viewport prop not terminal size
  (#37g/#39), and keep the composer visible within the viewport (#56).
- Title-truncation and review/checks glyph mapping tests (#37h).

## Tests
- `src/ui/components/pr_render_tests.rs` (new) — 13 behavioral tests.
- `src/ui/components/pr_list.rs` — 3 tests added inside the existing `#[cfg(test)]`
  module (truncation + glyph distinctness). No production code changed.
- `src/ui/components/mod.rs` — `#[cfg(test)]` registration only.

## Verification
- fmt / clippy -D warnings / complexity clippy / check-clippy-allows /
  check-architecture / build: all green.
- `cargo test`: RED trio failed as designed (resolved GREEN in P14); all other
  tests pass.
- All new tests/helpers carry `@plan`/`@requirement`/`@pseudocode` markers.
- Boundary clean: tests import only `crate::{domain,layout,state,pr_detail_content}`.
