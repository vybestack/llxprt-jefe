# P07 — GitHub Client TDD (RED) — COMPLETE

Plan: PLAN-20260624-PR-MODE.P07
Phase: GitHub Client TDD (RED)
Status: COMPLETE (RED achieved as required)

## Deliverables
- New pure seam stub `build_pr_comments_query(with_cursor: bool) -> String` in src/github/parse_pr.rs (returns String::new() in RED; #[must_use]; markers @plan P07 / @requirement REQ-PR-010 / @pseudocode component-002 lines 102-107). Implemented in P08; consumed by list_pr_comments.
- Re-exported build_pr_comments_query via the pub use parse_pr::{...} block in src/github/mod.rs.
- 29 behavioral PR tests appended to src/github/tests.rs with 5 inline r#"..."# fixtures (PR_LIST_SEARCH_JSON, PR_DETAIL_JSON, PR_COMMENTS_JSON, PR_CREATED_COMMENT_JSON, PR_ROLLUP_HETEROGENEOUS_JSON) + sample_pr_detail() helper.

## RED Evidence
- cargo test: 356 passed; 26 failed (all PR-scoped, all assertion-mismatch at tests.rs assert sites; zero production unwrap/index panics).
- Critical contract tests present: list_pr_comments_query_targets_pull_request_not_issue; heterogeneous CheckRun+StatusContext rollup; draft/review/checks qualifiers + deterministic order; parse_pr_state merged detection; degraded-not-dropped malformed review/check; build_pr_send_payload with/without focused comment.

## Green Gates (non-test)
- cargo fmt --all --check: OK
- cargo clippy --workspace --all-targets --all-features -- -D warnings: OK
- bash scripts/check-clippy-allows.sh: OK
- bash scripts/check-architecture.sh: OK
- cargo build --workspace --all-features --locked: OK

## Hard Gates
- rg banned smells (assert!(true)/#[ignore]/.unwrap()/.expect()) in src/github/: NONE
- No #[allow] added; no diff to Cargo.toml [lints]/clippy.toml/.github/clippy/clippy.toml
- Scope: only src/github/{mod.rs,parse_pr.rs,tests.rs} changed; .llxprt/ untouched
