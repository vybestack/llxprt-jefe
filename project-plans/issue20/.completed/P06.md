# P06 — GitHub Client Stub — COMPLETE

- **Plan ID:** PLAN-20260624-PR-MODE
- **Phase ID:** P06
- **Timestamp:** 2026-06-24 (issue20 branch)
- **Prerequisite:** .completed/P05A.md (PASS) ✓

## Files changed
- CREATED `src/github/parse_pr.rs` — 11 TOTAL-STUB free functions (build_pr_search_args, build_pr_search_query, parse_pull_requests_json, parse_pull_request_detail_json, parse_pr_review, parse_pr_check, parse_review_decision, parse_check_status, parse_checks_rollup, parse_pr_state, sort_pull_requests). Boundary-isolated imports: crate::domain, serde_json::Value, super::{GhError, PrListResponse}.
- MODIFIED `src/github/mod.rs` — `mod parse_pr;` + `pub use parse_pr::{…}` re-exports; `struct PrListResponse` (derive Default, mirrors IssueListResponse); `struct PrSendPayload` (derive Default, structured owned fields mirroring SendPayload); 6 new GhClient stub methods (list_pull_requests, get_pull_request_detail, list_pr_comments, create_pr_comment, open_pull_request_in_browser, build_pr_send_payload).

## Build result
- cargo fmt --all --check: PASS
- cargo clippy --workspace --all-targets --all-features -- -D warnings: PASS (0)
- cargo build --workspace --all-features --locked: PASS (0 warnings)
- cargo test --workspace --all-features --locked: PASS — 641 (353 lib + 89 doc + 199 integration), 0 failed
- bash scripts/check-clippy-allows.sh: PASS
- bash scripts/check-architecture.sh: PASS

## Isolation check
- Boundary HARD gate: no `crate::ui` / `crate::state` / `crate::app_input` imports in `src/github/`. PASS.
- TOTAL-STUB HARD gate: no `todo!()` / `unimplemented!()` in `src/github/mod.rs` or `src/github/parse_pr.rs`. PASS.
- Config gate: no diff to Cargo.toml [lints] / clippy.toml / .github/clippy/clippy.toml. PASS.

## Semantic summary
- All bodies are total, panic-free stubs returning deterministic wrong/empty/default/Err values (no real logic; deferred to P08).
- `list_pr_comments` exists as a NEW PR-specific method (distinct from issue `list_comments`) — its real query will target `repository.pullRequest(number:).comments` (NOT `repository.issue`), per P00A §2d grounding.
- `create_pr_comment` documented to use the issues REST comment endpoint (valid for PR numbers).
- Reuses GhError / CommentsResponse / IssueComment / parse_comments_json / parse_page_info / parse_created_comment_json (no new error enum).
- Per-fn @plan/@requirement/@pseudocode traceability markers on every new item (11 in parse_pr.rs, 8 in mod.rs).

## Design notes
- IssueComment and PullRequestDetail do NOT derive Default → create_pr_comment / get_pull_request_detail / parse_pull_request_detail_json return deterministic `Err(GhError::ParseError("stub"))` instead of fabricating a struct.
- Helper visibility escalated to `pub` + `pub use` re-export (mirrors how parse.rs functions are exposed) to avoid dead_code under -D warnings without any #[allow].
- build_pr_send_payload mirrors build_send_payload as a static method (no &self).

## Verification: P06A
- rustreviewer verdict: **Phase 06: PASS**, no findings.
