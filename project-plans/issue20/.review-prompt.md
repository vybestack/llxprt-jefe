You are reviewing a multi-phase implementation PLAN (documentation only, no production code yet) for GitHub issue #20 in the Rust project `vybestack/llxprt-jefe` (a tmux/PTY agent-orchestration TUI). The plan adds a GitHub Pull Request mode to the app, entered with the `p` key, mirroring the existing Issues mode (entered with `i`).

All plan artifacts are under the absolute directory `/Users/acoliver/projects/jefe-base/branch-1/project-plans/issue20/`. Read every file there.

## Your task
Perform a rigorous, complete review of the PLAN for correctness, executability, and standards compliance. Determine whether this plan, if executed exactly as written, would produce a correct, high-quality, standards-compliant PR-mode feature.

## Authoritative reference materials (read these)
- The feature requirements: run `gh issue view 20` and `gh issue view 46` (closed sibling). The plan must satisfy issue #20's scope.
- The standards the plan must enforce:
  - `/Users/acoliver/projects/jefe-base/branch-1/dev-docs/PLAN.md`
  - `/Users/acoliver/projects/jefe-base/branch-1/dev-docs/PLAN-TEMPLATE.md`
  - `/Users/acoliver/projects/jefe-base/branch-1/dev-docs/COORDINATING.md`
  - `/Users/acoliver/projects/jefe-base/branch-1/dev-docs/RULES.md`
  - `/Users/acoliver/projects/jefe-base/branch-1/dev-docs/project-standards.md`
- The proven exemplar plan to compare structure/rigor against: `/Users/acoliver/projects/jefe-base/branch-1/project-plans/issue15/`
- The CURRENT source tree under `/Users/acoliver/projects/jefe-base/branch-1/src/`. Verify the plan's claimed integration points (file paths, function names, line numbers, enum names, module structure) actually match the current code. The plan must integrate with current, post-refactor patterns (typed message bus, async gh I/O wrapper, shared scroll/layout modules, current `AppState::apply*` reducer, `handle_normal_key_event`, `input_mode_for_state`).

## Review dimensions (all must hold)
1. Requirement completeness: every part of issue #20 scope is covered by an expanded, testable requirement with GIVEN/WHEN/THEN behavior contracts. No scope gaps.
2. Codebase accuracy: claimed files/functions/line numbers/enums/modules exist as described in current `src/`. Flag any stale or wrong references.
3. Architecture & boundaries: additive (no parallel `*_v2` forks); respects module ownership (UI renders/emits intent; state/event layer owns transitions; gh client is an isolated boundary; persistence owns file I/O). PR mode mirrors current Issues-mode patterns, not stale buggy ones.
4. TDD discipline: strict Stub -> TDD(RED) -> Impl(GREEN) per slice; tests verify behavior not internals; no mock theater; RED genuinely fails before GREEN.
5. Phase coordination: strict sequential numbering with paired worker + verifier phases; prerequisite gates; completion markers; verifier output contract requires BEHAVIORAL code-reading evidence (cited file:line), runtime-path reachability (key -> route -> event -> dispatch -> reducer -> render), contradiction scans, atomic PASS/FAIL.
6. Traceability: per-file `@plan`/`@requirement`/`@pseudocode` markers; impl phases cite pseudocode line ranges; REQ->Phase->Pseudocode matrix is complete and consistent.
7. Pseudocode quality: numbered, algorithmic, with validation/error/ordering/side-effects; impl phases reference real line ranges that exist in the pseudocode files.
8. Quality gates: every phase declares the verification baseline (cargo fmt check, clippy -D warnings, build --locked, test --locked, plus the clippy-allows gate). Deferred-implementation grep gates present in impl phases.
9. HARD CONSTRAINT — no overrides: the plan must FORBID any first-party clippy allow attribute (`#[allow(clippy::...)]`, `#![allow(...)]`, `cfg_attr(..., allow(clippy::...))`) and must FORBID raising clippy.toml thresholds (cognitive-complexity 15, function lines 60, args 6, type-complexity 250, max-struct-bools 3). The plan must require functions/modules be split small enough to satisfy these thresholds WITHOUT overrides. Verify this is enforced in the verification phases.
10. Regression-avoidance: the Issues mode had follow-on bugs the PR plan must design around from the start. Verify each is explicitly addressed by a named requirement AND a verification check:
    - Comment composer focus & auto-scroll consistency (opening composer sets the correct detail subfocus; composer is visible in viewport; viewport follows after create).
    - Filter controls fully interactive (every field updates draft state; Apply refreshes list).
    - List is scroll-aware with selection-following (no clipping); reuses the shared scroll abstraction rather than a one-off.
    - List rendering matches loaded data exactly (no items silently dropped); regression coverage.
    - Repo-pane Up/Down navigation works in PR mode (focus tracked by the PR focus enum, not pane_focus); shared repo-nav helper.
    - Non-blocking async gh I/O (no sync gh calls on the UI thread; reuse the async wrapper); no silent None drops; no duplicated layout constants; scroll overflow derived from actual rendered length; viewport height passed as a prop; long titles truncated; repo slug validated.
    - Zero clippy-allow suppressions and zero complexity/length overrides (as in dimension 9).
11. Integration & access path: explicit who-calls-what, user trigger path (`p` from dashboard), state migration (transient `prs_state`, serde default if persisted), backward-compat (existing flows unaffected).
12. Internal consistency: file cross-references, phase numbering, traceability matrix, and execution tracker are mutually consistent; no contradictions; no missing referenced files.

## Output format (required)
- A verdict line: exactly `REVIEW VERDICT: PASS` or `REVIEW VERDICT: FAIL`.
  - PASS only if the plan is executable and standards-compliant with at most pedantic/cosmetic nits remaining (nothing that would cause a wrong, low-quality, or non-compliant implementation).
  - FAIL if there is any substantive defect: scope gap, wrong/stale codebase reference, broken TDD/coordination, missing regression guard, missing/weak quality gate, or any path that permits clippy-allow/complexity overrides.
- A numbered list of findings. For each: severity (BLOCKER / MAJOR / MINOR / PEDANTIC), the exact file and section, what is wrong, and a concrete required correction. Cite file:line evidence from the plan and/or current src where relevant.
- If PASS, still list any remaining PEDANTIC nits.

Be thorough and skeptical. Ground every finding in evidence from the actual files.